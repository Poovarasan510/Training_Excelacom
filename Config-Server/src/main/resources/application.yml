server:
 port: 8888
 
spring:
 cloud:
  config:
   server:
    git:
     uri: https://github.com/Poovarasan510/Cloud_Configure.git
     search-paths:
         - '{application}'
       
management:
 endpoints:
  web:
    exposure:
     include:
     - "*"